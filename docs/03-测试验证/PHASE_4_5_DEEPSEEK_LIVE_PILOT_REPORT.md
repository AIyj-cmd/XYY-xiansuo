# 阶段 4.5 DeepSeek 单用户真实联调：独立测试验证报告

日期：2026-07-31

基线：`validation/phase4-deepseek-live-pilot` /
`7c7097ae42bbc8fa9aa913fb18f355500b5215a0`

结论：**真实单用户 Mock 投递链路执行成功，但因 1 个未解决 P2，不满足
“P1/P2/P3 均为零才完全通过”的门禁，不允许标记为完全通过。**

## 验证边界

- 本轮只读核验指定隔离数据库、冻结设计、相关实现和 Git 状态。
- 未读取或记录 Key、JWT、Prompt、上下文、客户数据、AI/通知正文、receipt
  正文或错误正文。
- 未发起网络或 Provider 请求，未启动 API、Scheduler 或 Worker，未修改
  源码、迁移、数据库或配置，未提交 Git。
- 仅记录 secret 权限为 `0600`，不记录其他 secret 元数据。

## 真实联调链路事实

- 试点用户：启用中的 `member`，用户 ID `2`；业务日期：`2026-07-31`。
- dry-run 候选数：`5`。
- DeepSeek：`deepseek-v4-flash`，一次成功，输入/输出 token
  `1049/93`，fallback `0`。
- AI 审计记录唯一且为 `completed`，关联唯一 outbox；结果临时快照已按设计
  清除。
- Worker 启动前两次队列预检均为 `SAFE`，且均只有本次 pilot 任务。
- 唯一 Mock 通知为 `sent`，operation 无重复；receipt SHA-256：
  `902f784efd28112d1018263cca6e6b31bae359a14e395394b50b8fde65b8ffc3`。
- 源数据 hash 前后不变；所有项目进程已停止；临时 JWT 已删除。

只读复核确认迁移 `001` 至 `005` 与冻结实现一致，数据库完整性检查为 `ok`、
无外键违规；到期任务唯一启用，其余相关规则关闭；AI 专用查询保持
`member/self` 数据隔离。当前 Git 基线与指定提交一致，唯一工作区变化为本
报告。

## 未解决问题

### P2：真实 Provider 延迟未进入持久化审计

冻结设计要求管理员 AI 日志返回安全元数据、token、延迟、Prompt 版本和通知
关联。静态核验确认 Provider 成功结果计算并向上返回 `latencyMs`，但审计保存
契约和数据库表均没有对应字段，两类 Scheduler 任务也未将该值传入持久化层。
因此本次真实请求的精确延迟无法从审计记录取得，且不得推算或补写。

该问题不否定已经发生的单条 Provider 成功、唯一 outbox、Mock 投递成功、
权限隔离或数据完整性，但缺失冻结设计要求的关键可观测性和真实联调验收
证据，等级为 **P2（中等）**。

严重级别汇总：

- P1：`0`
- P2：`1`
- P3：`0`

## 准入与提交结论

- 链路执行：**成功**。
- 完全通过：**否**。
- 无条件验收或继续扩大灰度：**不允许**。
- 本报告：**可以作为“链路成功但验收阻塞”的真实记录提交**；不得改写为
  完全通过。本验证代理未执行 Git 提交。
- 按既定门禁，联调过程中不得边跑边修改代码；本轮未实施修复。

## 下一步最小修复边界

1. 新增向前兼容的增量迁移，为 AI 审计增加可空、非负受约束的
   `latency_ms`；不得修改已执行的迁移 `005`。
2. 仅贯通 Provider 成功结果至审计保存、两类 Scheduler 任务和 admin 安全
   投影；不改变业务口径、Prompt、Provider 请求、通知或权限逻辑。
3. 补充迁移兼容、持久化约束、真实/Fake 成功路径、两类任务传递和 admin
   脱敏投影测试，并执行受影响的后端构建、测试及 H5 基线构建。
4. 修复经独立验证后，在新的隔离副本重新执行单用户受控联调，保留精确
   `latency_ms`、主库/WAL/SHM 前后零变化、dry-run、紧邻 Worker 的 `SAFE`
   和唯一 Mock `sent` 证据。既有请求的延迟无法追溯，不得补造。
