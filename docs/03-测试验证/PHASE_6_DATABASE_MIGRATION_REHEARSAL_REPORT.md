# 阶段六数据库迁移副本演练报告

结论：**代码级验证通过；生产副本演练未执行，不能判定通过。**

## 已完成的受控验证

- 后端 `npm ci`、`npm run build`、`npm test` 均通过；测试共 `121/121` 通过。
- 迁移相关测试覆盖版本 `001–006`、顺序执行、重复执行、校验和不匹配阻断、失败事务回滚、完整性校验及外键校验。
- 当前迁移 checksum：`001 c10d…57a0`、`002 db949…b0d9`、`003 e774…1704`、`004 61ab…f75`、`005 8636…6346`、`006 b6b27…026a`。完整值见阶段六生产就绪审计。

这些结果仅针对受控测试库和代码，不代表任一生产数据库、备份或恢复介质。

## 未执行的生产副本演练

以下前置输入或授权均未提供/确认：实际生产项目与数据库、生产数据库只读一致性备份授权、备份目录、恢复目录及可验证的旧制品。因此未执行：

1. 对真实生产数据库制作包含 WAL 已提交数据的一致性备份；
2. 在该备份副本执行 `001–006` migration-copy；
3. 在独立恢复位置执行 restore-copy；
4. 对恢复副本复核 `schema_migrations`、`PRAGMA integrity_check` 和 `PRAGMA foreign_key_check`；
5. 将恢复副本配合旧生产制品完成兼容性与回滚演练。

未在生产库、疑似生产路径或本机 `server/data` 上运行迁移、备份、恢复、清理或写入命令。

## 重新演练的通过标准

获授权后，必须在维护窗口外的真实一致性备份副本上按下列顺序执行，并保留不含客户数据的结果摘要：

1. 记录基线制品版本、SQLite 版本、备份时间点、主库/WAL 一致性证据和 `001–006` checksum；
2. 仅对副本启动新制品并执行迁移；不得手工插入、删除或修改 `schema_migrations`；
3. 核对 `schema_migrations` 恰有 `001–006` 且 checksum 全部匹配；
4. 核对 `PRAGMA integrity_check=ok`、`PRAGMA foreign_key_check` 为空，并核查必要的 AI/通知表与默认关闭规则；
5. 从同一备份独立 restore-copy，复做完整性、外键和登录/只读 API 冒烟；
6. 在不删数据、不做 down migration 的前提下，用已确认旧生产制品演练回退兼容性；
7. 记录实际 Nginx/PM2 核验结果后，才可将本报告状态改为“生产副本演练通过”。

任一步失败或输入缺失均为上线门禁，不得以代码测试替代。
