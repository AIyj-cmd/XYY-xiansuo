# 项目代理与软件工程工作流

本文件适用于整个仓库。更深目录中的 `AGENTS.md` 或
`AGENTS.override.md` 可以为其子树增加更具体的规则。

## 项目约束

- 技术栈保持为 uni-app（Vue 3 + TypeScript + Vite）、Fastify、
  `node:sqlite`、Zod v4。除非用户明确批准，不得更换技术栈。
- 界面文字使用简体中文。
- 所有前端请求统一走 `app/src/utils/request.ts`，不得直接使用
  `fetch` 或 axios。
- 不新增需求之外的第三方生产依赖；确有必要时先说明理由、替代方案和
  影响并等待确认。
- `PRD.md` 存在时，以它和用户当前明确指令为需求依据；当前仓库没有
  `PRD.md` 时，以用户当前指令、`docs/00-项目说明/README.md` 和真实代码行为为依据，不得
  臆造缺失需求。
- 工作区可能包含用户已有的未提交改动。所有代理必须先检查 git 状态，
  保护无关改动，不得使用破坏性命令覆盖、清理或回滚它们。
- `docs/01-审计与设计/SYSTEM_ANALYSIS.md`、`docs/01-审计与设计/TECH_DESIGN.md`、
  `docs/01-审计与设计/DATABASE_CHANGE_PLAN.md`、`docs/01-审计与设计/API_CHANGE_PLAN.md`
  和 `docs/01-审计与设计/DEVELOPMENT_PLAN.md` 只能作为待核验上下文；
  除非用户或主代理明确确认，不自动视为本次任务的批准设计。

## 按风险选择工作流和模型

主代理先依据改动范围、数据风险、权限风险、可逆性和跨模块程度分类。

### 低风险任务

示例：文案、样式、明确的小范围修复、无数据和权限影响的机械修改。

- 默认由 `implementer`（gpt-5.6-terra / high）实现。
- 完成后由 `test_verifier` 验证，或在范围极小时由主代理运行等价验证。
- 不强制生成完整设计文档，但必须说明范围、验证结果和剩余风险。
- 纯重复测试执行或结构化报告整理，可显式选择
  gpt-5.6-luna / medium。

### 中风险任务

示例：常规功能、多个文件联动、局部 API 调整、可回滚数据库变更。

按顺序执行：

1. `audit_designer`（gpt-5.6-sol / high）。
2. 用户确认设计，或用户已明确授权按设计端到端执行。
3. `implementer`（gpt-5.6-terra / high）。
4. `test_verifier`（gpt-5.6-terra / high）。
5. `acceptance_optimizer`（gpt-5.6-sol / high）。

### 高风险任务

以下任一情况默认属于高风险：

- 认证、授权、角色、数据隔离或敏感信息。
- 数据迁移、删除、批量更新、备份恢复或不可逆操作。
- 支付、并发、事务、状态机或跨系统契约。
- 大规模重构、基础设施、生产部署或安全关键变更。

必须执行完整四阶段。审计设计和最终验收可显式升级为
gpt-5.6-sol / xhigh。复杂实现可将 `implementer` 的显式 spawn 模型升级为
gpt-5.6-sol / high。除非用户明确授权端到端执行，审计阶段结束后应停下，
让用户确认设计再进入实现。

## 四阶段职责与门禁

### 第一阶段：审计与技术设计

调用 `audit_designer`。

- 本阶段只读，不修改代码或文档。
- 审计代理返回结构化设计包，由主代理展示给用户。
- 需要落盘时，由主代理在用户确认后统一写入 `docs/01-审计与设计/`；不得在根目录新增 Markdown 文件。
- 必须覆盖需求、架构、数据、API、权限、安全、部署、回滚和任务拆分。
- 必须引用真实文件、符号、接口和数据结构。
- 设计未明确时不得进入实现。

### 第二阶段：开发实现

调用 `implementer`。

- 只实施已经批准且边界明确的任务。
- 写入型代理不得并行修改同一工作区。
- 每个实施任务完成后返回修改范围、影响和验证结果。
- 发现设计冲突或需要扩展范围时，返回主代理重新决策。

### 第三阶段：测试验证

调用 `test_verifier`。

- 独立验证功能、权限、异常、安全、数据和兼容性。
- 默认不得修改业务源码；允许测试代码、报告和测试工具产物。
- 测试前后检查 git 状态和差异，防止把已有改动误归因给测试阶段。
- 输出 `docs/03-测试验证/TEST_REPORT.md`。
- 严重失败或关键测试未运行时，不得声明完成或无条件进入验收。

### 第四阶段：验收优化

调用 `acceptance_optimizer`。

- 对照原始需求、批准设计、实现差异和测试报告验收。
- 只修复已确认、可复现且位于批准范围内的问题。
- 修复后重新运行受影响的验证。
- 输出 `docs/04-验收交付/ACCEPTANCE_REPORT.md`、
  `docs/02-开发实现/CHANGELOG.md`、
  `docs/04-验收交付/DEPLOYMENT_NOTES.md` 和
  `docs/04-验收交付/ROLLBACK_PLAN.md`，并明确上线建议和残余风险。

## 并行规则

- 并行优先用于只读探索、文档核验、测试分析、日志分析和互不依赖的检查。
- 审计、实现、测试、验收四个主阶段存在前后依赖，默认顺序执行。
- 禁止多个写入型代理同时修改同一代码工作区。
- 主代理负责汇总子代理结论、解决冲突和向用户报告最终结果。

## 项目验证基线

后端：

```bash
cd server
npm run build
npm test
```

前端 H5：

```bash
cd app
npm run build:h5
```

仅当微信小程序目标受影响时：

```bash
cd app
npm run build:mp-weixin
```

依赖发生变化时，运行适用的生产依赖审计。无法运行的验证必须说明原因，
不能写成通过。
